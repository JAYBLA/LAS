extends layout

block content
    .container
      .div#flash
        div.col.md-12
          if successes
            for success in successes
              div.alert.alert-success #{ success }
          if errors
            for error in errors
              div.alert.alert-danger #{ error }
